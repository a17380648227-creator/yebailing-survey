# 🚀 火山云部署教程

## 野百灵问卷系统 - 火山云迁移完整指南

本教程将帮助你把问卷系统从当前主机迁移到火山云服务器。

---

## 📋 目录

1. [准备工作](#准备工作)
2. [数据导出（旧主机）](#数据导出旧主机)
3. [火山云服务器配置](#火山云服务器配置)
4. [上传项目文件](#上传项目文件)
5. [数据导入（新主机）](#数据导入新主机)
6. [域名配置（可选）](#域名配置可选)
7. [常见问题](#常见问题)

---

## 准备工作

### 需要的东西

1. ✅ 火山云账号（已注册并实名认证）
2. ✅ 一台云服务器（推荐配置：1核2G，操作系统：Ubuntu 20.04 或 CentOS 7+）
3. ✅ FTP工具（推荐：FileZilla）或使用 SSH 命令行
4. ✅ 旧主机上的所有数据

### 预计时间

- 🕐 数据导出：5分钟
- 🕐 服务器配置：15分钟
- 🕐 文件上传：10分钟
- 🕐 数据导入：5分钟
- **总计：约 35 分钟**

---

## 数据导出（旧主机）

### 步骤 1：在旧主机导出数据

1. **打开旧主机的网站**（你现在正在使用的）

2. **访问数据导出工具**
   ```
   https://你的旧网址/export-data.html
   ```

3. **点击"下载数据文件"按钮**
   - 会下载一个 `data-backup-xxxxxxx.json` 文件
   - 妥善保存这个文件，等会儿要用

4. **确认导出的数据**
   - 查看文件，确认包含：
     - ✅ 门店信息
     - ✅ 问卷反馈
     - ✅ 账户信息

> 💡 **提示**：导出的 JSON 文件可以用记事本打开查看，但不要修改内容

---

## 火山云服务器配置

### 步骤 2：购买和配置服务器

#### 2.1 购买云服务器

1. 登录火山云控制台：https://console.volcengine.com/

2. 进入 **云服务器 ECS** 产品页面

3. 点击 **创建实例**，选择配置：
   - **地域**：选择离你最近的（如：华北2-北京）
   - **实例规格**：1核2G（够用了）
   - **操作系统**：Ubuntu 20.04 LTS（推荐）或 CentOS 7
   - **公网IP**：必须选择（分配公网IP或弹性IP）
   - **安全组**：放行 80 端口（HTTP）和 443 端口（HTTPS）

4. 设置 **登录密码**（记住这个密码！）

5. 点击 **立即购买**

#### 2.2 配置安全组（重要！）

1. 进入 **安全组管理**

2. 添加入站规则：
   ```
   协议：TCP
   端口：80
   源地址：0.0.0.0/0
   说明：HTTP访问
   ```

   ```
   协议：TCP
   端口：443
   源地址：0.0.0.0/0
   说明：HTTPS访问
   ```

   ```
   协议：TCP
   端口：22
   源地址：0.0.0.0/0
   说明：SSH远程登录
   ```

#### 2.3 安装 Web 服务器

有两种方案，选择其中一种即可：

**方案 A：使用 Nginx（推荐，轻量级）**

1. SSH 连接到服务器（Windows 用 PuTTY，Mac/Linux 用终端）
   ```bash
   ssh root@你的服务器IP
   # 输入密码
   ```

2. 更新系统
   ```bash
   # Ubuntu
   apt update && apt upgrade -y

   # CentOS
   yum update -y
   ```

3. 安装 Nginx
   ```bash
   # Ubuntu
   apt install nginx -y

   # CentOS
   yum install nginx -y
   ```

4. 启动 Nginx
   ```bash
   systemctl start nginx
   systemctl enable nginx
   ```

5. 测试是否成功
   - 在浏览器访问：`http://你的服务器IP`
   - 如果看到 Nginx 欢迎页面，说明成功了！

**方案 B：使用 Apache（备选方案）**

```bash
# Ubuntu
apt install apache2 -y
systemctl start apache2
systemctl enable apache2

# CentOS
yum install httpd -y
systemctl start httpd
systemctl enable httpd
```

---

## 上传项目文件

### 步骤 3：上传 HTML 文件到服务器

#### 3.1 确定网站根目录

**Nginx 默认目录：**
- Ubuntu: `/var/www/html/`
- CentOS: `/usr/share/nginx/html/`

**Apache 默认目录：**
- Ubuntu: `/var/www/html/`
- CentOS: `/var/www/html/`

#### 3.2 方法 A：使用 FTP 工具上传（推荐新手）

1. **下载 FileZilla**
   - https://filezilla-project.org/

2. **连接到服务器**
   - 主机：`sftp://你的服务器IP`
   - 用户名：`root`
   - 密码：你设置的服务器密码
   - 端口：`22`

3. **上传文件**

   进入右侧的服务器目录：`/var/www/html/`

   从本地上传以下**核心文件**：

   **必传文件（后台管理）：**
   - ✅ `admin.html` - 后台首页
   - ✅ `stores.html` - 门店管理
   - ✅ `data.html` - 数据统计
   - ✅ `feedbacks.html` - 反馈查看
   - ✅ `qrcode.html` - 二维码生成
   - ✅ `questionnaire-editor.html` - 问卷编辑器

   **必传文件（顾客端）：**
   - ✅ `survey.html` - 问卷页面

   **必传文件（数据迁移）：**
   - ✅ `export-data.html` - 数据导出工具
   - ✅ `import-data.html` - 数据导入工具

   **必传文件（账户管理）：**
   - ✅ `login.html` - 登录页面
   - ✅ `accounts.html` - 账户管理
   - ✅ `auth.js` - 认证脚本

   **必传文件（其他功能）：**
   - ✅ `backup.html` - 数据备份
   - ✅ `网站配置.html` - 网站配置
   - ✅ `navbar.js` - 导航栏脚本

   **必传文件夹：**
   - ✅ `pic/` - 图片资源文件夹（包含轮播图等）

4. **设置文件权限**

   在 SSH 中执行：
   ```bash
   cd /var/www/html/
   chmod -R 755 *
   chown -R www-data:www-data *  # Ubuntu Nginx
   # 或
   chown -R nginx:nginx *         # CentOS Nginx
   # 或
   chown -R apache:apache *       # CentOS Apache
   ```

#### 3.3 方法 B：使用 SCP 命令上传（推荐熟手）

在你的本地电脑（Mac/Linux 终端）：

```bash
# 上传整个项目文件夹
scp -r /Users/liuyun/Desktop/桌面二维码/* root@你的服务器IP:/var/www/html/

# 或者单独上传文件
scp admin.html survey.html root@你的服务器IP:/var/www/html/
```

Windows 用户可以使用 WinSCP 工具。

#### 3.4 方法 C：使用 Git 部署（推荐开发者）

```bash
# 在服务器上执行
cd /var/www/html/
git clone https://github.com/你的用户名/你的仓库名.git .
```

---

## 数据导入（新主机）

### 步骤 4：在火山云服务器恢复数据

1. **访问新服务器的网站**
   ```
   http://你的服务器IP/import-data.html
   ```

2. **上传数据文件**
   - 点击"选择文件"或拖拽
   - 选择之前导出的 `data-backup-xxxxxxx.json` 文件

3. **确认要导入的数据**
   - ✅ 导入门店信息
   - ✅ 导入问卷反馈
   - ✅ 导入账户信息

4. **点击"开始导入数据"**
   - 等待进度条完成
   - 看到成功提示

5. **刷新页面并测试**
   ```
   http://你的服务器IP/admin.html
   ```
   - 检查门店是否正常显示
   - 检查数据统计是否正确

---

## 域名配置（可选）

### 步骤 5：绑定自己的域名

如果你想用 `www.你的餐厅.com` 代替 IP 访问：

#### 5.1 购买域名

1. 在火山云或其他域名商购买域名
2. 完成域名实名认证

#### 5.2 添加域名解析

在域名管理后台：

```
记录类型：A
主机记录：@
记录值：你的服务器IP
TTL：10分钟
```

```
记录类型：A
主机记录：www
记录值：你的服务器IP
TTL：10分钟
```

#### 5.3 配置 Nginx

编辑 Nginx 配置文件：

```bash
nano /etc/nginx/sites-available/default
# 或
nano /etc/nginx/nginx.conf
```

修改 `server` 块：

```nginx
server {
    listen 80;
    server_name 你的域名.com www.你的域名.com;
    root /var/www/html;
    index admin.html index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

重启 Nginx：

```bash
nginx -t  # 测试配置是否正确
systemctl restart nginx
```

#### 5.4 配置 HTTPS（推荐）

使用免费的 Let's Encrypt 证书：

```bash
# 安装 Certbot
apt install certbot python3-certbot-nginx -y

# 自动配置 HTTPS
certbot --nginx -d 你的域名.com -d www.你的域名.com

# 自动续期
certbot renew --dry-run
```

---

## 常见问题

### Q1: 上传后访问 IP 显示 Nginx 欢迎页面？

**原因**：文件没有上传到正确目录

**解决**：
```bash
# 检查文件是否存在
ls -la /var/www/html/

# 如果没有 admin.html，重新上传文件
```

### Q2: 访问页面显示 403 Forbidden？

**原因**：文件权限不对

**解决**：
```bash
cd /var/www/html/
chmod -R 755 *
chown -R www-data:www-data *  # Ubuntu
```

### Q3: 数据导入后看不到数据？

**原因**：没有刷新页面或浏览器缓存

**解决**：
- 按 `Ctrl + Shift + R` 强制刷新
- 或清除浏览器缓存后重新打开

### Q4: 二维码生成后扫码打不开？

**原因**：二维码里的网址还是旧地址

**解决**：
1. 进入 `qrcode.html` 页面
2. 重新生成二维码（会自动使用新网址）
3. 重新打印二维码

### Q5: 轮播图不显示？

**原因**：`pic/` 文件夹没有上传

**解决**：
```bash
# 检查图片是否存在
ls -la /var/www/html/pic/

# 重新上传 pic 文件夹
scp -r pic/ root@你的服务器IP:/var/www/html/
```

### Q6: 想在新旧主机同时使用怎么办?

**数据不同步问题**：
- ⚠️ 新旧主机的数据是独立的（localStorage 是浏览器本地存储）
- 解决方案 1：只使用一个主机，关闭旧主机
- 解决方案 2：定期手动导出导入数据同步
- 解决方案 3：搭建后端服务器实现实时同步（需要开发）

### Q7: 如何配置后台管理密码？

访问新服务器：
```
http://你的服务器IP/login.html
```

首次访问会提示设置管理员密码。

### Q8: 火山云服务器如何续费？

在火山云控制台 → 云服务器 → 选择实例 → 续费

建议购买包年套餐更便宜。

---

## 🎉 部署完成检查清单

部署完成后，逐一检查：

- [ ] 能访问 `http://服务器IP/admin.html`
- [ ] 后台能看到门店数据
- [ ] 后台能看到问卷反馈数据
- [ ] 能访问 `http://服务器IP/survey.html?store=xxx&table=1`
- [ ] 轮播图正常显示
- [ ] 能提交问卷并在后台看到
- [ ] 能生成新的二维码
- [ ] 二维码能正常扫码跳转
- [ ] 能导出 Excel 数据

---

## 📞 需要帮助？

如果遇到问题：

1. **检查服务器日志**
   ```bash
   # Nginx 错误日志
   tail -f /var/log/nginx/error.log

   # Apache 错误日志
   tail -f /var/log/apache2/error.log
   ```

2. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 标签页的错误信息

3. **防火墙问题**
   ```bash
   # 检查防火墙状态
   ufw status

   # 如果防火墙开启，允许 80 和 443 端口
   ufw allow 80/tcp
   ufw allow 443/tcp
   ```

---

## 📚 相关文档

- [野百灵问卷系统 - 使用指南](./CLAUDE.md)
- [新手必看 - 完整部署教程](./【新手必看】完整部署教程.md)
- [数据永久保存指南](./📖使用指南-数据永久保存.md)

---

## 🔄 版本更新

### v1.0 (2024-11-14)
- ✨ 初始版本：火山云部署完整教程
- 📝 包含数据迁移、服务器配置、域名绑定

---

**祝你部署顺利！🎊**

如有问题，随时联系技术支持。
